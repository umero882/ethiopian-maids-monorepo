import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

/**
 * BookingGraphQLTest - Test component for GraphQL booking operations
 * This is a placeholder component for production builds
 */
const BookingGraphQLTest = () => {
  return (
    <Card className="max-w-2xl mx-auto mt-8">
      <CardHeader>
        <CardTitle>Booking GraphQL Test</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-gray-500">
          This test component is only available in development mode.
        </p>
      </CardContent>
    </Card>
  );
};

export default BookingGraphQLTest;
