import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

/**
 * CommunicationGraphQLTest - Test component for GraphQL communication
 * This is a placeholder component for production builds
 */
const CommunicationGraphQLTest = () => {
  return (
    <Card className="max-w-2xl mx-auto mt-8">
      <CardHeader>
        <CardTitle>Communication GraphQL Test</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-gray-500">
          This test component is only available in development mode.
        </p>
      </CardContent>
    </Card>
  );
};

export default CommunicationGraphQLTest;
