import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

/**
 * AnalyticsGraphQLTest - Test component for GraphQL analytics operations
 * This is a placeholder component for production builds
 */
const AnalyticsGraphQLTest = () => {
  return (
    <Card className="max-w-2xl mx-auto mt-8">
      <CardHeader>
        <CardTitle>Analytics GraphQL Test</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-gray-500">
          This test component is only available in development mode.
        </p>
      </CardContent>
    </Card>
  );
};

export default AnalyticsGraphQLTest;
