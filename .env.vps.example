# VPS Deployment Configuration
# Copy this file to .env.vps and fill in your values
# NEVER commit .env.vps to git!

# VPS Connection
VPS_HOST=your-vps-ip-address
VPS_USER=root
VPS_PASSWORD=your-secure-password
VPS_SSH_KEY_PATH=~/.ssh/id_rsa_ethiopian_maids

# Deployment Paths
VPS_WEB_DIR=/var/www/ethiopian-maids
VPS_HASURA_DIR=/opt/ethiopian-maids-hasura

# Optional: SSH Port (default 22)
VPS_SSH_PORT=22
