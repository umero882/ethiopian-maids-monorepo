overwrite: true
schema:
  - https://api.ethiopianmaids.com/v1/graphql:
      headers:
        x-hasura-admin-secret: ${HASURA_ADMIN_SECRET}
generates:
  schema.graphql:
    plugins:
      - schema-ast
